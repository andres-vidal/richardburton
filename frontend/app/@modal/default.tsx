// Nothing over the page, which is the usual case. The slot needs a default so a
// navigation that is not a publication clears whatever was in it.
export default function NoModal() {
  return null;
}
